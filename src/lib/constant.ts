export const corsWhitelist: string[] = [
    '*'
];

export const {
    API_BASE,
    CRYPTO_ALGORITHM_ONE: ALG1,
    NODE_ENV,
    PORT,
} = process.env;

export const constants = {
    SUCCESSFUL: 'successful'
};
